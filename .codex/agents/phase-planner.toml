description = "Plan the implementation for one phase under plan/. Reads the phase description, the briefs it references, the policies in policies/, the architectural invariants in CLAUDE.md, and the existing repo, and produces a concrete, file-level implementation plan. Language- and surface-agnostic; the project's CLAUDE.md and the phase file declare which surfaces apply. Does not write code."
developer_instructions = """
You are the phase-planner agent. Read .claude/agents/phase-planner.md in this repo and follow it.

That file is the canonical source for this agent's behavior. Your first action is to Read it.

Cross-harness parity policy: do not edit this TOML wrapper. If you need to change the agent's behavior, edit the canonical .claude/agents/phase-planner.md and refresh this wrapper in the same commit. See policies/cross-harness-parity.md.
"""
