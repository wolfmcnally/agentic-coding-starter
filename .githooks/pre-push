#!/usr/bin/env bash
# Optional repository-managed enforcement. Install with ./bin/install-hooks.

set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"
if ./bin/check-receipt pre-push; then
  exit 0
fi
exec ./bin/check all
