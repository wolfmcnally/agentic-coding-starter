#!/usr/bin/env bash
# Fast deterministic policy checks before a local commit is accepted.

set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"
./bin/check-harness-parity
./bin/check-toolchain-callers
./bin/test-governance validate
